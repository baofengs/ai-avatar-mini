<navigation-bar title="Ai" back="{{false}}" background="linear-gradient(to bottom, #06bdfc, transparent)" />
<!-- <navigation-bar title="Ai" back="{{false}}" background="rgba(249, 249, 249)" /> -->
<view class="content">
  <view class="item"></view>
</view>
<!-- <view class="section">Custom Page Route Example</view>
<block wx:for="{{list}}" wx:key="id">
  <view
    class="item"
    bind:tap="goNextPage"
    data-route-type="{{item.routeType}}"
    data-next-route-type="{{item.nextRouteType}}"
    data-disable-drag="{{item.disableDrag}}"
    data-fullscreen="{{item.fullscreen}}"
    >{{item.content}}
    </view>
</block> -->
